개인 콘텐츠 채널의 발행 상태 관리 도구. 발행 여부를 사람 기억에 의존하다가
오판·중복 노출 사고가 나서, 상태를 시스템이 들게 만들었습니다.

## 문제 정의

콘텐츠를 혼자 기획하고 만들고 올리다 보면, 각 에피소드가 지금 어느 단계에 있는지를
결국 머릿속에서만 관리하게 됩니다. 소재를 모아둔 곳, 대본을 쓴 곳, 실제로 올린 곳이
전부 흩어져 있으니 "이건 올렸던가?"를 판단할 근거가 기억밖에 없습니다.

그러다 사고가 났습니다.

- **오판**: 제작만 해두고 발행하지 않은 건을 발행했다고 착각해 그대로 묻어버림
- **중복 노출**: 이미 다룬 소재를 다시 다뤄서 같은 내용을 두 번 내보냄
- **취소 이력 증발**: 발행을 접은 건에 대해 왜 접었는지가 남지 않아, 나중에 같은 판단을 반복

세 가지 모두 원인이 같습니다. **상태를 사람이 들고 있었다는 것.**

그래서 이 도구는 기능을 늘리는 대신 딱 하나만 합니다.
에피소드의 상태와 그 근거를 시스템에 기록하고, 판단은 기록을 보고 하게 만드는 것.

## 데이터 모델

### Episode — 발행 단위 콘텐츠 1건

| 필드 | 설명 |
|---|---|
| `number` | 에피소드 번호 (고유) |
| `title` | 제목 |
| `category` | AI소식 / 두드려봄 / 기회 / 꿀팁 |
| `status` | 제작중 / 제작완료 / 발행완료 / 발행취소 |
| `published_at` | 발행일 (미발행 시 비어 있음) |
| `cancel_reason` | 취소사유 (취소 건이 아니면 비어 있음) |
| `created_at` | 생성일시 |

`status`가 이 프로젝트의 핵심입니다. **제작완료와 발행완료를 분리한 것**이 의도적인
설계인데, 오판 사고가 정확히 이 둘을 뭉뚱그린 데서 나왔기 때문입니다. 만들어 둔 것과
내보낸 것은 다른 상태이고, 시스템은 이 둘을 절대 같은 칸에 두지 않습니다.
`cancel_reason`도 같은 이유로 존재합니다. 접었다는 사실만으로는 다음 판단에
도움이 되지 않고, 왜 접었는지가 남아야 같은 소재를 다시 집어들 때 근거가 됩니다.

### Topic — 에피소드가 다루는 사건/주제

| 필드 | 설명 |
|---|---|
| `name` | 사건/주제 이름 (예: "AI 평가장 사고", 고유) |
| `episodes` | 이 사건을 다룬 에피소드들 (M2M) |
| `created_at` | 생성일시 |

**노출 횟수를 세는 단위는 소재가 아니라 사건입니다.** 이게 이 모델의 핵심입니다.
서로 다른 URL 여러 개가 같은 사건을 다루는 것이 실제 중복 노출 사고의 형태였기
때문에, URL 단위로 세면 정작 문제가 된 경우가 그대로 통과합니다.

`exposure_count`는 연결된 에피소드 중 **발행완료만** 셉니다. 제작만 해둔 건과
발행을 접은 건은 밖으로 나가지 않았으므로 노출이 아닙니다. 중복 노출의 기준은
'만들었는가'가 아니라 '내보냈는가'입니다.

에피소드는 여러 사건을 다룰 수 있고 사건도 여러 에피소드에 걸치므로 M2M입니다.
접힌 에피소드도 사건에 연결된 채로 남습니다. 그래야 "이건 왜 접었나"를
취소사유 텍스트가 아니라 조회로 답할 수 있습니다.

### Source — 에피소드의 근거가 되는 원본 자료

| 필드 | 설명 |
|---|---|
| `url` | 원본 URL |
| `collected_at` | 수집일시 |
| `summary` | 요약 |
| `episode` | 연결된 에피소드 (아직 없으면 비어 있음) |

소재는 에피소드보다 먼저 쌓입니다. 그래서 `episode`는 nullable입니다.
아직 어디에도 쓰지 않은 소재가 정상 상태이고, 나중에 에피소드가 만들어질 때 연결됩니다.
이미 어떤 URL을 다뤘는지가 기록으로 남아야 "이거 전에 했던 건가?"를 기억이 아니라
조회로 답할 수 있습니다.

`episode` 삭제는 `PROTECT`로 막혀 있습니다. 근거가 붙은 에피소드를 지우면 그
소재를 다뤘다는 이력이 조용히 사라지는데, 이력이 소리 없이 증발하는 경로를
막는 것이 이 테이블의 목적이기 때문입니다. 지우려면 근거를 먼저 정리해야 합니다.

### Deadline — 마감이 있는 기회 정보

| 필드 | 설명 |
|---|---|
| `title` | 제목 |
| `due_date` | 마감일 |
| `official_url` | 공식링크 |
| `episode` | 연결된 에피소드 (아직 없으면 비어 있음) |

'기회' 카테고리는 다른 카테고리와 성질이 다릅니다. 늦으면 가치가 0이 됩니다.
그래서 마감일을 별도 모델로 분리했습니다. 아직 에피소드로 만들지 않은 마감 건도
독립적으로 추적되어야 하므로 여기서도 `episode`는 nullable입니다.

## 로드맵

상태를 기록할 그릇을 먼저 만들고, 그 위에 판단을 얹는 순서로 갑니다.
그릇 쪽은 끝났습니다. 남은 일은 지금 손으로 하는 사건 분류를 자동화하는 것입니다.

**완료 — 발행 상태 전이 검증**

`제작중 → 제작완료 → 발행완료` 순방향만 허용하고, 발행취소는 제작완료에서만
가능합니다. 발행완료에는 발행일이, 발행취소에는 취소사유가 없으면 전이가 막힙니다.
오판 사고의 원인이 상태 칸이 아무렇게나 바뀔 수 있었다는 데 있었기 때문에,
기록의 신뢰도를 여기서 먼저 확보했습니다.

**완료 — 마감 D-day 계산**

`Deadline`의 남은 일수를 저장하지 않고 조회 시점에 계산합니다.
`days_left` · `is_expired` · `d_day_label`(`D-18` / `D-DAY` / `마감`)을 제공하고,
`Deadline.objects.active()`로 미마감 건만 급한 순서대로 가져옵니다.

D-day를 컬럼으로 저장하지 않은 이유는 날짜가 지나면 곧바로 틀린 값이 되기 때문입니다.
저장된 D-day는 갱신을 누가 책임지느냐는 문제를 새로 만들고, 그건 이 프로젝트가
없애려는 바로 그 종류의 문제입니다. 기준일은 `settings.TIME_ZONE`(Asia/Seoul)이며,
UTC로 계산하면 한국 시간 자정부터 오전 9시 사이에 하루가 어긋납니다.

**완료 — Topic 엔티티 (사건 단위 노출 추적)**

노출 횟수를 셀 수 있는 단위를 만들었습니다. 사건별로 몇 번 내보냈는지가
`topic.exposure_count`로 답해지고, 접힌 에피소드도 사건에 연결된 채 남아
"무엇을 다루려다 접혔는지"가 조회로 복원됩니다.

ep8이 그 예입니다. 취소 사유에 자연어로만 적혀 있던 판단 근거가 이제
`ep8.topics` → `topic.episodes`로 따라갈 수 있는 형태가 됐습니다.

**완료 — 중복 감지 (임베딩 기반 유사도 판정)**

새 소재가 이미 다룬 사건과 겹치는지 후보를 띄웁니다.

```bash
python manage.py check_topic "AI 평가장에서 또 사고가 났다"
```

기존 `Topic`의 이름과 그 사건을 다룬 에피소드 제목들을 합쳐 임베딩하고, 입력과의
코사인 유사도로 상위 3개를 유사도 점수·노출 횟수와 함께 보여줍니다. 이름만으로는
표현이 짧아 비슷한 사건이 구분되지 않아서, 실제로 그 사건을 어떤 말로 다뤘는지를
같이 넣습니다.

모델은 `BAAI/bge-m3`이고 로컬에서 돕니다. 아직 발행하지 않은 기획이 외부 API로
나가는 것이 껄끄럽고, 소재를 넣을 때마다 호출되는 성격이라 비용도 맞지 않습니다.
GPU가 있으면 쓰고 없으면 CPU로 폴백합니다. 임베딩은 `Topic`에 캐시되며 이름이나
연결된 에피소드가 바뀌면 자동으로 다시 계산됩니다. 이 규모에 벡터DB는 과합니다.

**판정은 참고용입니다.** 이 명령은 아무것도 바꾸지 않습니다. 후보만 출력하고
`Topic` 연결과 승격은 사람이 결정합니다. 애초에 이 프로젝트의 목적이 판단을
자동화하는 게 아니라, 판단할 근거를 기억 밖에 두는 것이기 때문입니다.

**완료 — 스캔 공정과 연동 (`scan_check`)**

아침 스캔은 후보를 한 건씩 내놓지 않습니다. 2~3개를 한 번에 올려놓고 그중 뭘 할지
고르는 자리라, 한 건씩 조회하면 **후보끼리 비교가 안 됩니다.**

```bash
python manage.py scan_check "후보1" "후보2" "후보3"
python manage.py scan_check < 후보목록.txt        # 한 줄에 하나
```

```
후보                              1위 Topic             유사도  노출  참고
AI 평가장에서 또 사고가 났다      AI 평가장 사고         1.000    1회  노출 1회 · 1위 뚜렷 — 중복 검토
qwen 차기 모델 출시 임박          Qwen3.8-Max 출시       1.000    1회  노출 1회 · 1위 뚜렷 — 중복 검토
엔비디아 신형 칩 공급 계약        AI 평가장 사고         0.000    1회  노출 1회 · 유사도 낮음 — 참고
```

공정 안에서의 자리는 이렇습니다:

> **스캔 후보 → `scan_check` → 판정 참고 → 사람이 결정**

마지막 화살표가 핵심입니다. `scan_check`는 `check_topic`과 마찬가지로 **아무것도
바꾸지 않습니다.** 참고 라벨을 붙일 뿐 어느 후보를 할지는 사람이 정합니다.

**참고 라벨도 절대값으로 자르지 않습니다.** 유사도 하나만 보면 임계값을 정하고 싶어지는데
이 판정은 그런 성격이 아니라, 노출 횟수와 **1위·2위 간격**을 같이 봅니다. 1위가 2위를
크게 따돌리면 "중복 검토", 접전이면 "사람 확인"으로 갈립니다. 노출이 0회면 이미 다룬
적이 없다는 뜻이므로 유사도와 무관하게 "신규 소재"입니다.

**다음 — 판정 품질 올리기**

지금은 제목과 사건 이름만 봅니다. `Source`의 요약까지 넣고, URL 정규화 후 완전
일치 같은 싼 검사를 앞단에 두는 것이 다음 순서입니다. 임베딩으로도 애매한 구간에
LLM 판정을 얹는 것은 그다음입니다.

## 알려진 한계

노출은 Episode 외에 키트(정적 페이지)에서도 발생하나, 현재 스키마는 Episode 노출만
추적한다. 키트 노출 추적은 범위 외.

## 시작하기

```bash
python -m venv venv
venv\Scripts\activate          # macOS/Linux: source venv/bin/activate
pip install -r requirements.txt

copy .env.example .env         # macOS/Linux: cp .env.example .env
# .env를 열어 DJANGO_SECRET_KEY 값을 채웁니다.
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"

python manage.py migrate
python manage.py loaddata initial_episodes    # ep1~10 실데이터 시드 (선택)
python manage.py createsuperuser
python manage.py runserver
```

`http://127.0.0.1:8000/admin/` 에서 네 모델을 모두 다룰 수 있습니다.

### 중복 감지 (선택)

`check_topic` · `scan_check` 두 명령만 임베딩 모델이 필요합니다. 기본 설치에는 포함되지 않습니다.

```bash
pip install -r requirements-ml.txt
python manage.py check_topic "AI 평가장에서 또 사고가 났다"   # 한 건 깊게
python manage.py scan_check "후보1" "후보2" "후보3"          # 여러 건 넓게
```

`torch`가 무겁고 나머지 기능은 전부 이것 없이 동작하므로 분리해 두었습니다.
설치하지 않으면 이 두 명령만 안내를 띄우고 멈추며, 나머지는 그대로 돌아갑니다.

비밀값은 전부 `.env`로 관리하며 코드에 하드코딩하지 않습니다.
`.env`는 커밋되지 않고, `.env.example`만 저장소에 포함됩니다.

## 실사용 예시

2026-08-09, Meta 건을 다룰지 판단한 기록입니다.

```bash
python manage.py check_topic "Meta AI 모델이 사이버보안 테스트 중 타사 시스템 해킹, 세 번째 회사"
```

```
1. AI 평가장 사고  유사도 0.429  노출 1회
     ep6 AI 평가장 사고 2건 [노출]
     ep8 AI 사고 상세 보고서 [발행취소]
2. Qwen3.8-Max 출시  유사도 0.348  노출 1회
     ep7 Qwen3.8-Max [노출]
```

**판정 — 후속편 발행 가능.** 같은 계열(1위 Topic)이지만 Episode 노출은 1회(ep6)뿐이고,
Meta 건은 동일 사건 재탕이 아니라 새 사건(세 번째 회사)입니다. ep8은 "같은 사건 반복
노출"이라 접었는데, 이번 건은 그 사유에 해당하지 않습니다.

**유사도는 절대값이 아니라 순위와 간격으로 읽습니다.** 0.429가 절대적으로 높아서
위험하다는 뜻이 아니라, 2위(0.348)와 0.08밖에 벌어지지 않았다는 것이 "이 입력이 특정
사건에 확실히 붙지는 않는다"는 신호입니다. 1위가 2위를 크게 따돌릴 때 그 사건과 같은
건일 가능성이 높고, 그때 노출 횟수를 함께 봐야 합니다.

## 기술 스택

Django 6.1 · SQLite · Python 3.14

혼자 쓰는 도구이고 데이터도 많지 않아 SQLite로 시작합니다.
DB를 바꿔야 할 이유가 실제로 생기기 전까지는 바꾸지 않습니다.
